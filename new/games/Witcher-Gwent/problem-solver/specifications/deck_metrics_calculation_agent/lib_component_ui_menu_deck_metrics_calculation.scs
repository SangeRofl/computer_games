lib_component_ui_menu_deck_metrics_calculation
	=> nrel_main_idtf:
		[Компонент библиотеки. Команда пользовательского интерфейса расчёта метрик колод] (* <- lang_ru;; *);
		[Library component. User interfaces command of calculating deck metrics] (* <- lang_en;; *);

	<- library_of_platform_independent_reusable_components;
	<- library_of_atomic_reusable_components;;

lib_component_ui_menu_deck_metrics_calculation = [*^"file://ui_menu_deck_metrics_calculation.scsi"*];;
